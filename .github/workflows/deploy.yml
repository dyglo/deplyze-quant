name: Deploy

# ─── Trigger strategy ──────────────────────────────────────────────────────
on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      skip_gateway:
        description: 'Skip gateway deployment (hosting only)'
        type: boolean
        default: false
      skip_hosting:
        description: 'Skip hosting deployment (gateway only)'
        type: boolean
        default: false
      skip_quant_engine:
        description: 'Skip quant engine deployment'
        type: boolean
        default: false
      skip_backtest_engine:
        description: 'Skip backtest engine deployment'
        type: boolean
        default: false
      skip_firestore:
        description: 'Skip Firestore rules + indexes deployment'
        type: boolean
        default: false

# ─── Concurrency ───────────────────────────────────────────────────────────
concurrency:
  group: deploy-production
  cancel-in-progress: false

env:
  GCP_PROJECT_ID:      deplyze-quant
  GCP_REGION:          us-central1
  CLOUD_RUN_SERVICE:   deplyze-gateway
  AR_REPOSITORY:       services
  AR_IMAGE:            deplyze-gateway
  FIREBASE_PROJECT_ID: deplyze-quant

jobs:
  # ─── 0. Path detection ─────────────────────────────────────────────────
  detect-changes:
    name: Detect changed paths
    runs-on: ubuntu-latest
    outputs:
      gateway:   ${{ steps.filter.outputs.gateway }}
      frontend:  ${{ steps.filter.outputs.frontend }}
      firestore: ${{ steps.filter.outputs.firestore }}
      quant:     ${{ steps.filter.outputs.quant }}
      backtest:  ${{ steps.filter.outputs.backtest }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            gateway:
              - 'cloud-run/gateway/**'
            frontend:
              - 'src/**'
              - 'index.html'
              - 'vite.config.*'
              - 'tsconfig*.json'
              - 'package*.json'
              - 'components.json'
            firestore:
              - 'firestore.rules'
              - 'firestore.indexes.json'
              - 'firebase.json'
            quant:
              - 'services/quant-engine/**'
            backtest:
              - 'deplyze-backtest-engine/**'

  # ─── 1. Deploy Cloud Run Gateway ───────────────────────────────────────
  deploy-gateway:
    name: Deploy · Gateway (Cloud Run)
    runs-on: ubuntu-latest
    needs: detect-changes
    if: |
      (needs.detect-changes.outputs.gateway == 'true' || github.event_name == 'workflow_dispatch') &&
      github.event.inputs.skip_gateway != 'true'
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Ensure Artifact Registry repository exists
        run: |
          gcloud artifacts repositories describe ${{ env.AR_REPOSITORY }} \
            --location=${{ env.GCP_REGION }} \
            --project=${{ env.GCP_PROJECT_ID }} \
            --quiet 2>/dev/null \
          || gcloud artifacts repositories create ${{ env.AR_REPOSITORY }} \
            --repository-format=docker \
            --location=${{ env.GCP_REGION }} \
            --project=${{ env.GCP_PROJECT_ID }} \
            --description="Docker images for Deplyze Quant services" \
            --quiet
          echo "Artifact Registry repository: ${{ env.AR_REPOSITORY }} ✓"

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.GCP_REGION }}-docker.pkg.dev --quiet

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: cloud-run/gateway
          push: true
          tags: |
            ${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.AR_REPOSITORY }}/${{ env.AR_IMAGE }}:${{ github.sha }}
            ${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.AR_REPOSITORY }}/${{ env.AR_IMAGE }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ env.CLOUD_RUN_SERVICE }} \
            --image "${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.AR_REPOSITORY }}/${{ env.AR_IMAGE }}:${{ github.sha }}" \
            --region ${{ env.GCP_REGION }} \
            --platform managed \
            --allow-unauthenticated \
            --memory 512Mi \
            --cpu 1 \
            --min-instances 0 \
            --max-instances 5 \
            --timeout 60 \
            --vpc-connector ${{ secrets.VPC_CONNECTOR_NAME }} \
            --vpc-egress private-ranges-only \
            --set-env-vars "\
          FIREBASE_PROJECT_ID=${{ env.FIREBASE_PROJECT_ID }},\
          GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }},\
          FINNHUB_API_KEY=${{ secrets.FINNHUB_API_KEY }},\
          ALPHA_VANTAGE_API_KEY=${{ secrets.ALPHA_VANTAGE_API_KEY }},\
          TWELVE_DATA_API_KEY=${{ secrets.TWELVE_DATA_API_KEY }},\
          TAVILY_API_KEY=${{ secrets.TAVILY_API_KEY }},\
          SERPER_API_KEY=${{ secrets.SERPER_API_KEY }},\
          POLYGON_API_KEY=${{ secrets.POLYGON_API_KEY }},\
          FMP_API_KEY=${{ secrets.FMP_API_KEY }},\
          EODHD_API_KEY=${{ secrets.EODHD_API_KEY }},\
          SEC_EDGAR_USER_AGENT=${{ secrets.SEC_EDGAR_USER_AGENT }},\
          REDIS_HOST=${{ secrets.REDIS_HOST }},\
          REDIS_PORT=${{ secrets.REDIS_PORT }},\
          QUANT_ENGINE_URL=${{ secrets.QUANT_ENGINE_URL }},\
          BACKTEST_ENGINE_URL=${{ secrets.BACKTEST_ENGINE_URL }},\
          PERSONALIZATION_ENABLED=true" \
            --set-secrets "PERSONALIZATION_USER_ID_SALT=personalization-user-id-salt:latest"

      - name: Verify gateway health
        run: |
          SERVICE_URL=$(gcloud run services describe ${{ env.CLOUD_RUN_SERVICE }} \
            --region ${{ env.GCP_REGION }} \
            --format 'value(status.url)')
          echo "Gateway URL: $SERVICE_URL"
          for i in $(seq 1 12); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" "$SERVICE_URL/health" || echo "000")
            echo "Attempt $i/12: HTTP $STATUS"
            if [ "$STATUS" = "200" ]; then echo "Gateway healthy ✓"; exit 0; fi
            sleep 5
          done
          echo "Gateway health check timed out" && exit 1

  # ─── 2. Deploy Firestore Rules & Indexes ───────────────────────────────
  deploy-firestore:
    name: Deploy · Firestore
    runs-on: ubuntu-latest
    needs: detect-changes
    if: |
      (needs.detect-changes.outputs.firestore == 'true' || github.event_name == 'workflow_dispatch') &&
      github.event.inputs.skip_firestore != 'true'
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Firebase CLI
        run: npm install -g firebase-tools@latest

      - name: Deploy Firestore rules and indexes
        run: |
          firebase deploy --only firestore \
            --project ${{ env.FIREBASE_PROJECT_ID }} \
            --non-interactive

  # ─── 3. Build & Deploy Firebase Hosting ────────────────────────────────
  deploy-hosting:
    name: Deploy · Hosting (Firebase)
    runs-on: ubuntu-latest
    needs: [detect-changes, deploy-gateway]
    if: |
      always() &&
      (needs.deploy-gateway.result == 'success' || needs.deploy-gateway.result == 'skipped') &&
      (needs.detect-changes.outputs.frontend == 'true' || github.event_name == 'workflow_dispatch') &&
      github.event.inputs.skip_hosting != 'true'
    permissions:
      contents: read
      id-token: write
    env:
      VITE_FIREBASE_API_KEY:             ${{ secrets.VITE_FIREBASE_API_KEY }}
      VITE_FIREBASE_AUTH_DOMAIN:         ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
      VITE_FIREBASE_PROJECT_ID:          ${{ secrets.VITE_FIREBASE_PROJECT_ID }}
      VITE_FIREBASE_STORAGE_BUCKET:      ${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}
      VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}
      VITE_FIREBASE_APP_ID:              ${{ secrets.VITE_FIREBASE_APP_ID }}
      VITE_FIREBASE_MEASUREMENT_ID:      ${{ secrets.VITE_FIREBASE_MEASUREMENT_ID }}
      VITE_GATEWAY_URL:                  ${{ secrets.VITE_GATEWAY_URL }}
      VITE_DEV_GATEWAY_URL:              ${{ secrets.VITE_GATEWAY_URL }}

    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Validate build environment
        run: |
          MISSING=()
          [ -z "$VITE_FIREBASE_API_KEY" ]    && MISSING+=("VITE_FIREBASE_API_KEY")
          [ -z "$VITE_FIREBASE_PROJECT_ID" ] && MISSING+=("VITE_FIREBASE_PROJECT_ID")
          [ -z "$VITE_FIREBASE_APP_ID" ]     && MISSING+=("VITE_FIREBASE_APP_ID")
          if [ ${#MISSING[@]} -gt 0 ]; then
            echo "::error::Missing mandatory build secrets: ${MISSING[*]}"
            exit 1
          fi
          echo "All mandatory build secrets are present ✓"

      - name: Build frontend
        run: npm run build

      - name: Install Firebase CLI
        run: npm install -g firebase-tools@latest

      - name: Deploy to Firebase Hosting
        run: |
          firebase deploy --only hosting \
            --project ${{ env.FIREBASE_PROJECT_ID }} \
            --non-interactive

      - name: Live URL
        run: echo "Deployed to https://${{ env.FIREBASE_PROJECT_ID }}.web.app ✓"

  # ─── 4. Deploy Quant Engine (Python / Cloud Run) ───────────────────────
  deploy-quant-engine:
    name: Deploy · Quant Engine (Cloud Run)
    runs-on: ubuntu-latest
    needs: detect-changes
    if: |
      (needs.detect-changes.outputs.quant == 'true' || github.event_name == 'workflow_dispatch') &&
      github.event.inputs.skip_quant_engine != 'true'
    permissions:
      contents: read
      id-token: write

    env:
      QE_SERVICE:  deplyze-quant-engine
      QE_AR_REPO:  deplyze-quant-engine
      QE_AR_IMAGE: deplyze-quant-engine

    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Ensure Artifact Registry repo exists
        run: |
          gcloud artifacts repositories describe ${{ env.QE_AR_REPO }} \
            --location=${{ env.GCP_REGION }} --project=${{ env.GCP_PROJECT_ID }} \
            --quiet 2>/dev/null \
          || gcloud artifacts repositories create ${{ env.QE_AR_REPO }} \
            --repository-format=docker \
            --location=${{ env.GCP_REGION }} \
            --project=${{ env.GCP_PROJECT_ID }} \
            --description="Quant engine Docker images" --quiet

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.GCP_REGION }}-docker.pkg.dev --quiet

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push quant engine image
        uses: docker/build-push-action@v5
        with:
          context: services/quant-engine
          push: true
          tags: |
            ${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.QE_AR_REPO }}/${{ env.QE_AR_IMAGE }}:${{ github.sha }}
            ${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.QE_AR_REPO }}/${{ env.QE_AR_IMAGE }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Deploy quant engine to Cloud Run
        run: |
          gcloud run deploy ${{ env.QE_SERVICE }} \
            --image "${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.QE_AR_REPO }}/${{ env.QE_AR_IMAGE }}:${{ github.sha }}" \
            --region ${{ env.GCP_REGION }} \
            --platform managed \
            --no-allow-unauthenticated \
            --memory 1Gi \
            --cpu 1 \
            --min-instances 0 \
            --max-instances 3 \
            --no-cpu-throttling \
            --timeout 300 \
            --vpc-connector ${{ secrets.VPC_CONNECTOR_NAME }} \
            --vpc-egress private-ranges-only \
            --set-env-vars "\
          GCP_PROJECT_ID=${{ env.GCP_PROJECT_ID }},\
          BIGQUERY_LOCATION=US,\
          QUANT_ENGINE_ENV=production,\
          GCS_BACKTEST_BUCKET=${{ secrets.BACKTEST_PARQUET_BUCKET }},\
          BACKTEST_PARQUET_OBJECT=backtest/wide_daily.parquet,\
          POLYGON_API_KEY=${{ secrets.POLYGON_API_KEY }},\
          FMP_API_KEY=${{ secrets.FMP_API_KEY }},\
          EODHD_API_KEY=${{ secrets.EODHD_API_KEY }},\
          FINNHUB_API_KEY=${{ secrets.FINNHUB_API_KEY }},\
          TWELVE_DATA_API_KEY=${{ secrets.TWELVE_DATA_API_KEY }},\
          ALPHA_VANTAGE_API_KEY=${{ secrets.ALPHA_VANTAGE_API_KEY }},\
          GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }},\
          TAVILY_API_KEY=${{ secrets.TAVILY_API_KEY }},\
          SERPER_API_KEY=${{ secrets.SERPER_API_KEY }},\
          EDGAR_USER_AGENT=${{ secrets.SEC_EDGAR_USER_AGENT }},\
          FRED_API_KEY=${{ secrets.FRED_API_KEY }},\
          REDIS_HOST=${{ secrets.REDIS_HOST }},\
          REDIS_PORT=${{ secrets.REDIS_PORT }},\
          PERSONALIZATION_ENABLED=true" \
            --set-secrets "PERSONALIZATION_USER_ID_SALT=personalization-user-id-salt:latest"

      - name: Verify quant engine health
        run: |
          SERVICE_URL=$(gcloud run services describe ${{ env.QE_SERVICE }} \
            --region ${{ env.GCP_REGION }} --format 'value(status.url)')
          echo "Quant Engine URL: $SERVICE_URL"
          TOKEN=$(gcloud auth print-identity-token)
          for i in $(seq 1 12); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
              -H "Authorization: Bearer $TOKEN" "$SERVICE_URL/health" || echo "000")
            echo "Attempt $i/12: HTTP $STATUS"
            if [ "$STATUS" = "200" ]; then echo "Quant Engine healthy ✓"; exit 0; fi
            sleep 5
          done
          echo "Quant Engine health check timed out" && exit 1

      - name: Ensure nightly backtest parquet export schedule
        run: |
          QE_URL=$(gcloud run services describe ${{ env.QE_SERVICE }} \
            --region ${{ env.GCP_REGION }} --format 'value(status.url)')
          JOB=backtest-export-nightly
          COMMON=(
            --location "${{ env.GCP_REGION }}"
            --schedule "0 2 * * 1-5"
            --time-zone "Etc/UTC"
            --uri "$QE_URL/pipelines/backtest/export-parquet"
            --http-method POST
            --message-body "{}"
            --oidc-service-account-email "${{ secrets.CLOUD_SCHEDULER_SA }}"
            --oidc-token-audience "$QE_URL"
            --attempt-deadline 300s
          )
          if gcloud scheduler jobs describe "$JOB" --location "${{ env.GCP_REGION }}" --quiet 2>/dev/null; then
            gcloud scheduler jobs update http "$JOB" "${COMMON[@]}" \
              --update-headers "Content-Type=application/json"
            echo "Updated scheduler job $JOB ✓"
          else
            gcloud scheduler jobs create http "$JOB" "${COMMON[@]}" \
              --headers "Content-Type=application/json"
            echo "Created scheduler job $JOB ✓"
          fi

  # ─── 4. Deploy Backtest Engine (Cloud Run, Rust) ───────────────────────
  deploy-backtest-engine:
    name: Deploy · Backtest Engine (Cloud Run)
    runs-on: ubuntu-latest
    needs: detect-changes
    if: |
      (needs.detect-changes.outputs.backtest == 'true' || github.event_name == 'workflow_dispatch') &&
      github.event.inputs.skip_backtest_engine != 'true'
    permissions:
      contents: read
      id-token: write

    env:
      BT_SERVICE:  deplyze-backtest-engine
      BT_AR_REPO:  deplyze-backtest-engine
      BT_AR_IMAGE: deplyze-backtest-engine

    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Ensure Artifact Registry repo exists
        run: |
          gcloud artifacts repositories describe ${{ env.BT_AR_REPO }} \
            --location=${{ env.GCP_REGION }} --project=${{ env.GCP_PROJECT_ID }} \
            --quiet 2>/dev/null \
          || gcloud artifacts repositories create ${{ env.BT_AR_REPO }} \
            --repository-format=docker \
            --location=${{ env.GCP_REGION }} \
            --project=${{ env.GCP_PROJECT_ID }} \
            --description="Backtest engine Docker images" --quiet

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.GCP_REGION }}-docker.pkg.dev --quiet

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push backtest engine image
        uses: docker/build-push-action@v5
        with:
          context: deplyze-backtest-engine
          push: true
          tags: |
            ${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.BT_AR_REPO }}/${{ env.BT_AR_IMAGE }}:${{ github.sha }}
            ${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.BT_AR_REPO }}/${{ env.BT_AR_IMAGE }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Deploy backtest engine to Cloud Run
        run: |
          gcloud run deploy ${{ env.BT_SERVICE }} \
            --image "${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.BT_AR_REPO }}/${{ env.BT_AR_IMAGE }}:${{ github.sha }}" \
            --region ${{ env.GCP_REGION }} \
            --platform managed \
            --no-allow-unauthenticated \
            --memory 2Gi \
            --cpu 2 \
            --min-instances 0 \
            --max-instances 3 \
            --no-cpu-throttling \
            --timeout 300 \
            --set-env-vars "\
          GCP_PROJECT_ID=${{ env.GCP_PROJECT_ID }},\
          BACKTEST_PARQUET_BUCKET=${{ secrets.BACKTEST_PARQUET_BUCKET }},\
          BACKTEST_PARQUET_OBJECT=backtest/wide_daily.parquet,\
          RUST_LOG=info"

      - name: Verify backtest engine health
        run: |
          SERVICE_URL=$(gcloud run services describe ${{ env.BT_SERVICE }} \
            --region ${{ env.GCP_REGION }} --format 'value(status.url)')
          echo "Backtest Engine URL: $SERVICE_URL"
          TOKEN=$(gcloud auth print-identity-token)
          for i in $(seq 1 12); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
              -H "Authorization: Bearer $TOKEN" "$SERVICE_URL/health" || echo "000")
            echo "Attempt $i/12: HTTP $STATUS"
            if [ "$STATUS" = "200" ]; then echo "Backtest Engine healthy ✓"; exit 0; fi
            sleep 5
          done
          echo "Backtest Engine health check timed out" && exit 1
