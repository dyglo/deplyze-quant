{
  "name": "deplyze-firestore-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Firestore security-rules tests (tenant isolation). Run against the Firebase emulator via `firebase emulators:exec`.",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.2"
  }
}
