# ─── Deplyze Quant Engine — Python Dependencies ────────────────────────────
# Pin major versions for reproducible builds.
# Update patch versions regularly for security fixes.

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Data validation
pydantic==2.10.4
pydantic-settings==2.7.0

# GCP
google-cloud-bigquery==3.27.0
google-cloud-bigquery-storage==2.27.0
google-cloud-storage==2.19.0
google-auth==2.37.0
pyarrow==18.1.0
db-dtypes==1.2.0

# HTTP client (async)
httpx==0.28.1
tenacity==9.0.0

# Data science core
pandas==2.2.3
numpy==2.2.1
scipy==1.15.0
statsmodels==0.14.4
scikit-learn==1.6.0

# Fast data processing (optional but highly useful)
polars==1.18.0

# Env / config
python-dotenv==1.0.1

# Structured logging
structlog==24.4.0

# Scheduling
apscheduler==3.10.4

# Utilities
python-dateutil==2.9.0
pytz==2024.2

# V3 Phase 2 — public-source refinery
feedparser==6.0.11        # RSS/Atom parsing for narrative ingestion
beautifulsoup4==4.12.3    # HTML parsing for document extraction
lxml==5.3.0               # fast XML/HTML backend for bs4
pypdf==5.1.0              # PDF text extraction (pure-Python, no system libs)

# Testing (keep in same file; pin to allow dev installs)
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1  # also used in tests
