/**
 * useAuth — convenience re-export from AuthProvider.
 *
 * Usage:
 *   import { useAuth } from '../hooks/useAuth';
 *   // or directly:
 *   import { useAuth } from '../components/AuthProvider';
 */
export { useAuth } from '../components/AuthProvider';
